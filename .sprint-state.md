# SPRINT: Immutable Replay Covenant + Final-HEAD Composition Closure

Turn the live grounded-plan proof + VerificationPolicyV2 into one truthful, immutable, final-HEAD self-canary
release candidate. ZERO paid calls — reuse retained architect/critic payloads (.private/closure-v2-*). No new
features / bake-off / threshold changes / deploy / push / fund / prod-migration. Do not weaken any gate.

## Phases (update as I go — NO speculative results)
- [ ] P0 — truth amendment (branch order; correct the "authenticated approval" overclaim). ZERO paid. COMMIT.
- [x] P1 — IMMUTABLE covenant: a required campaign HOLDS under off/shadow/unknown; env flag can't remove it.
- [x] P2 — activation FAIL-CLOSED: attach route consumes attach result; refuse before chain; attach before markLive.
- [x] P3 — ONE unified schema preflight (0026-0029 columns/table/index); SELF_CANARY+OBSERVE refuse if absent.
- [x] P4 — FRESH lease/generation-bound permits (CAS on runId; ≤5min age; stale→HOLD+re-run; late can't overwrite).
- [x] P5 — revalidate the PROBE itself (recomputed digest, source/URL/safety, 1:1 coverage).
- [x] P6 — ZERO-CALL final-HEAD composition proof using retained payloads (or stop composition_evidence_unavailable).
- [ ] P7 — final-head evidence artifact + founderCanaryReady gate + report.

## Retained payloads (verified present): .private/closure-v2-{architect-draft,critic-verdicts}.json (flash-lite)
## HEAD base: 5184894
