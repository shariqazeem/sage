# SPRINT: Real Canary Closure + Payout Action Replay

Capability sprint (NOT a bake-off). Close the grounded canary through the REAL job→revision→approval path;
compile deterministic MissionProbeV1 from grounded missions; persist+bind to the approved plan; make the
REAL payout pipeline replay a tester mission in a fresh guarded browser before settlement; prove replay is
SUBTRACTIVE (can veto, never manufacture a payout).

## Hard limits
- Critic model = google/gemini-3.1-flash-lite-preview ONLY. No Haiku/Pro/Sonnet/Opus/comparison.
- MAX 2 paid calls total (1 grounded architect + 1 V3 critic), one corrected canary runtime smoke. New ledger.
- Everything else deterministic or controlled-browser.
- NO real funding/deploy/settlement/payout. NO production push or flag enablement.
- REUSE existing guarded browser + inspection-replay. Do NOT build another browser/security subsystem.
- Do NOT modify payout thresholds; a replay result may only SUBTRACT (veto), never upgrade a decision.

## Phases (update as I go — NO speculative results)
- [x] P0 — amendment (canary truth) + source fixes A (operatorAuthorized), B (canaryPlanCommitment, not a token),
      C (grounded provenance on selected plan; quota/auth = inconclusive). COMMIT.
- [x] P1 — canary closure (LIVE 2-call attempt BLOCKED at canonical gate on fixture corpus; NOT ready; machinery proven deterministically): new 2-call ledger; corrected fixture through runInspectionJob→…→revision;
      then real approval service (recompute+approve). Only then founderCanaryReady=true. COMMIT.
- [ ] P2 — MissionProbeV1 + VerificationPolicyV1 pure deterministic compiler + rejection matrix. COMMIT.
- [ ] P3 — durable policy binding beside the approved revision; approval recomputes policyDigest; fail closed. COMMIT.
- [ ] P4 — PAYOUT_ACTION_REPLAY_MODE=off|shadow|canary in the real settlement path (after judge, before broadcast). COMMIT.
- [ ] P5 — idempotency + journaling (submissionId+policyDigest+probeDigest); no dup payout. COMMIT.
- [ ] P6 — proofs through real entrypoints (probe compiler / real-browser replay / runDeputyOnSubmission). COMMIT.
- [ ] P7 — demo:payout-replay vertical (real boundaries, fakes only at money/model edges). COMMIT.
- [ ] FINAL — patch + manifest + sanitized evidence + 16-item report.

## Paid-call ledger (cap 2) — record BEFORE each dispatch
- NEW ledger: promotion-evidence/canary-closure.ledger.json. 2-call smoke run (ledger records 3 dispatches incl 1 pre-fix iteration). CAP REACHED — no more paid.

## Key facts
- Prior sprint: canary selection mode shipped (mission-canary.ts). Paid smoke blocked at canonical gate on an
  EMPTY-scope harness defect; selection tail proven deterministically. THIS sprint closes it for real.
- bindCanaryApproval was a hash never consumed by approval → rename canaryPlanCommitment / remove; real approval
  path = createRevision(selected plan) → founder SIWE approve endpoint → recompute → approveRevision.
