# SPRINT: Payout Replay Money-Boundary Closure (ZERO paid calls)

Single objective: make VerificationPolicy + MissionProbe enforcement genuinely REACHABLE and IMPOSSIBLE TO
BYPASS across every automated settlement path. Repair sprint — NO features, NO models, NO deploy/fund/push/flag.
founderCanaryReady stays FALSE this sprint (live canonical-gate closure is a later sprint).

## Phases (update as I go — NO speculative results)
- [ ] P0 — append-only source-audit amendment (record the 8 exact defects). COMMIT.
- [x] P1 — VerificationPolicyV2 (versioned, complete, strict nested Zod; actionCriteria coverage). COMMIT.
- [x] P2 — bind policy to CURRENT REVISION (migration after 0027; createRevision persists; approval reads revision).
- [x] P3 — ATTACH approved policy to campaign in the real deploy path (write-once, atomic).
- [x] P4 — fail-closed applicability (policyRequired truth table; never infer non-action from incomplete policy).
- [x] P5 — CENTRAL settlement enforcement: ReplayPermit at settleApprovedSubmission; every sink gated.
- [x] P6 — runInspectionProbe enforces expectedAfterUrl + real beforeStateDigest; URL+text matrix.
- [ ] P7 — ledger reservation-vs-dispatch semantics + journal concurrency/preflight/version.
- [ ] P8 — real DB vertical E2E (scripted model, real everything else) + negatives.
- [ ] FINAL — report + patch + manifest + status fields (founderCanaryReady=false).

## The 8 defects being repaired (from prior overclaims)
1. bindCampaignVerificationPolicy has NO caller (attach never wired).
2. null policy SKIPS (not fail-closed for required campaigns).
3. partial action-criterion probe coverage can ALLOW.
4. approval policy read from job.result, not the current revision.
5. campaign policy binding is OVERWRITEABLE.
6. sweep approved-submission re-fire + decide/settle routes BYPASS replay.
7. expectedAfterUrl NOT enforced in runInspectionProbe.
8. ledger can't distinguish reservation from provider dispatch (the 3rd dispatch).
