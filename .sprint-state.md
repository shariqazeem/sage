# SPRINT: Authenticated Approval Route Closure (final narrow gate)

Prove POST /api/launch/[id]/approve with a GENUINELY VERIFIED SIWE session (no mocking of getSessionAddress /
session / founder / signature). ZERO paid calls, no chain calls. Then final self-canary release status.
Do NOT modify agent intelligence, replay logic, policy compilation, canonical gates, thresholds, or prompts.

## Phases
- [x] P0 — source-audit the real auth path (nonce → SIWE verify → session issue → cookie → getSessionAddress → approve).
- [x] P1 — real authenticated route test (ephemeral key, real nonce+SIWE+session cookie, real POST handler) + matrix.
- [ ] P2 — route-to-money composition (authenticated approval → attach → lease permit → sink → spy=1; negatives 0).
- [ ] P3 — final truth artifact + founderCanaryReady gate + patch + manifest + report.

## Constraints: no deploy/push/prod-migration/fund/settle/paid-inference. Never record key/secret/sig/cookie/payload.
