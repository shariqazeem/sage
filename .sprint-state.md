# SPRINT: Canonical Gate Closure + Self-Canary Release Candidate

Single objective: turn the existing grounded-generation + payout-replay implementation into ONE honestly
proven, deployable self-canary release candidate. Ends when EITHER (A) one real Flash-Lite grounded plan
crosses the REAL canonical gate → persists as current revision → real approval → policy attached → settlement-
spy replay demo; OR (B) a genuine model/product blocker isolated with exact deterministic evidence.

NO new subsystem (no CampaignLearning, vision, new critic, new browser, new security arch).

## Hard limits
- Critic/architect model = google/gemini-3.1-flash-lite-preview ONLY.
- Phase 0 = ZERO paid calls. Phase 3 = NEW ledger canary-runtime-closure-v2, HARD CAP 2 calls, only after 0-2 pass.
- No LLM call until the frozen candidate passes the gate DETERMINISTICALLY.
- NO deploy/fund/launch/settle/payout/push/flag-enable. Do NOT weaken any canonical gate.

## Phases (update as I go — NO speculative results)
- [x] P0 — forensic reconciliation (ledger 3 dispatches; recover exact gate rejection codes; offline replay;
      wrong-binding semantics; migration 0026/0027 explanation). ZERO paid. COMMIT.
- [ ] P1 — fix the canonical failure WITHOUT weakening the gate; regression test on the formerly-failing candidate.
- [ ] P2 — source-audit the verification-policy money boundary through PRODUCTION code; migrations on clean +
      previous schema; release preflight refusing canary mode when columns/tables missing.
- [ ] P3 — ONE clean live closure (≤2 paid, new ledger) OR deterministic rejection (outcome B).
- [ ] P4 — demo:self-canary (real boundaries; reproduce + 3 negative variants; spy counts).
- [ ] P5 — DARK/OBSERVE/SELF_CANARY configs + preflight command + operator runbook.
- [ ] P6 — final release gates + report (18 items) + attach prior+this patch/manifest/evidence.

## Paid-call ledger (Phase 3, cap 2) — NEW: canary-runtime-closure-v2. 0/2 used.

## Prior-sprint carryover (facts)
- Prior canary-closure.ledger.json recorded 3 dispatches (2-call cap BREACHED — one pre-fix harness iteration).
- Prior LIVE attempt blocked at canonical gate on the REPORTLY fixture (no_missions_passed_validation); the
  parsed architect draft was NOT retained → harness defect. Closure MACHINERY proven deterministically on the
  YARA fixture (canary:closure:det PASSES). P0-P7 of the payout-replay sprint all committed + green (1440).
